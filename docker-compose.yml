
version: '3.8'

services:
  fastapi-app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data   # persist users.json inside data/ folder
    environment:
      - ENVIRONMENT=development
    restart: unless-stopped
